# Production Environment Variables Example
# Rename this file to .env.prod and fill in your actual RDS credentials

# RDS Database Settings
RDS_USERNAME=your_rds_username
RDS_PASSWORD=your_rds_password
RDS_HOST=your-rds-endpoint.region.rds.amazonaws.com
RDS_PORT=5432
RDS_DB_NAME=quizapp

# JWT Settings
JWT_SECRET=your_production_jwt_secret_key

# Redis Settings (if you're using external Redis in production, update these)
# REDIS_HOST=your-redis-endpoint
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password
